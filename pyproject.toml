[project]
name = "dora-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.2.10",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]
